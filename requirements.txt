# ─────────────────────────────────────────────────────────────
# 90-Day Agentic AI course — Python dependencies
# Install:  pip install -r requirements.txt
# Days 1–3 need NOTHING from here (pure standard library).
# ─────────────────────────────────────────────────────────────

# Core (Phase 1+)
openai>=1.40            # unified client for OpenAI, Azure OpenAI, and Ollama
python-dotenv>=1.0      # load .env config
pydantic>=2.7           # structured output & validation
rich>=13.7              # pretty terminal output
requests>=2.31          # HTTP tools

# Notebooks — the daily lessons are Jupyter notebooks (.ipynb)
ipykernel>=6.29         # lets VS Code run the notebooks
nbformat>=5.10          # build & validate notebooks

# ── Installed later, per phase (uncomment when a day asks for it) ──
# Phase 3 — frameworks
openai-agents
langgraph
langchain
semantic-kernel
autogen-agentchat
autogen-ext[openai]
agent-framework            # Microsoft Agent Framework
mcp                        # Model Context Protocol SDK

# Phase 4 — retrieval / RAG
chromadb
faiss-cpu

# Phase 6 — evaluation / observability / serving
langfuse
deepeval
fastapi
uvicorn
